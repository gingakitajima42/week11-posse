# Week11 AIと考えたログ

## Part 1：AIと考えた1セッション

### テーマ

Week10で学習した fetch() の処理について、response.ok と response.json() をどの順番で使うのかを確認した。

### 送った指示（良いプロンプトの型）

目的: fetch() を使った処理の流れを理解し、コードを自分の言葉で説明できるようにしたい

前提: HTML/CSS/JavaScriptだけで書いている。Week10までに、非同期処理の概念、Promiseの概念、async/await、fetch()、response.ok、response.json()、JSON形式、try/catchを学んでいる

見てほしい観点: response.ok を確認する位置、response.json() に await を付ける理由、try/catch が担当する範囲

制約: Week10までの学習範囲を超える書き方やライブラリは使わない。完成コードを丸ごと作り直さず、考え方と確認ポイントを説明してほしい

出力形式: 「指摘」「理由」「自分で確認する方法」を箇条書きでまとめてほしい

確認したコード:

    async function loadMembers() {
      try {
        const response = await fetch("members.json");

        if (response.ok) {
          const members = await response.json();
          console.log(members);
        } else {
          console.log("データの取得に失敗しました");
        }
      } catch (error) {
        console.error(error);
      }
    }

### 返ってきた出力（要約）

- fetch() の結果を response に受け取り、まず response.ok を確認する。
- response.ok が false のときは、JSONを読み取らずにエラーのメッセージを表示する。
- response.json() はレスポンスの本文をJSONとして読み取る処理で、すぐに値が返るのではなくPromiseを返すため await を付ける。
- JSONを読み取った後で、取得したデータを使う処理を行う。
- 通信またはJSONの読み取りで問題が起きたときは catch の処理が実行される。

### "なぜ"を聞いた記録

- 聞いたこと: なぜ response.ok を response.json() より先に確認するのですか。また、なぜ response.json() に await が必要なのですか。
- 返ってきた説明（要約）: fetch() は通信自体ができた場合、失敗のレスポンスでもPromiseが成功した形で返ることがある。そのため、JSONを読み取る前に response.ok を確認して、成功したレスポンスかを判断する。response.json() はレスポンス本文を読み取る非同期処理で、結果が返るまで時間がかかるため、await で結果を待ってから members に入れる。
- 聞く前は分からなかったが、聞いて分かったこと: fetch() が返ってきたことと、取得に成功したことは同じではないと分かった。また、response.json() はJSONそのものではなく、JSONを読み取った結果を待つための処理だと説明できるようになった。

### 出力の4分類

| 分類 | 内容 | 判断理由 |
| --- | --- | --- |
| 採用する | response.ok を response.json() より前に確認する | Week10で学んだ内容に合っており、失敗したレスポンスを先に判断できるから |
| 採用する | response.json() に await を付け、結果を受け取ってからデータを使う | 非同期処理の完了を待つ流れを自分で説明でき、コードでも確認できるから |
| 保留する | エラーの種類ごとに表示内容を細かく分ける改善 | 目的には役立ちそうだが、今回は response.ok と try/catch の基本を確認するテーマなので、次の学習に回すから |
| 採用しない | 完成したコード全体をそのまま貼り替えて、理由を確認せずに動かす | 今週の目的はコードを自分の言葉で説明することなので、丸ごとの貼り替えでは自分の理解が残らないから |
| 要確認 | 自分が書いた別のコードでも、response.ok が false のときにJSONを読み取らずエラーメッセージを表示できるか | 説明だけでは実際の動作を確認できないため、失敗するレスポンスなどを使って自分で確かめる必要があるから |

## Part 2：流された場面の振り返り

Week07のTODOリストで、AIが提案したイベント処理を、addEventListener と classList の役割を十分に説明できないまま採用した。動いたのでそのまま進めたが、少し処理を変えるときにどこを直せばよいか迷った。

## Part 3：今後のAI利用方針

### Week11で気づいたこと

AIの説明が自然でも、コードが自分のものになったとは限らない。"なぜ"を聞いて、自分の言葉で説明できるかを確認してから採用する必要がある。

### 自分のAI利用ルール

1. インプットと深掘り問題ではAIを使わず、まず自分で考える。
2. AIには目的・前提・見てほしい観点・制約・出力形式を渡し、完成コードを丸ごともらわない。
3. 出力を採用する前に最低1回は"なぜ"を聞き、採用・保留・採用しない・要確認に仕分ける。個人情報やパスワードなどの機密情報は入力しない。
